import javax.swing.*;

public class Interface extends JFrame {
  public  Interface(){
      setTitle("Interface");
      setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Ativando a funcionalidade do X para fechar a tela.
      setSize(1024,728);
      setVisible(true);
      setResizable(false); // Desabilita a função de aumentar a tela ou diminuir.
      setLocationRelativeTo(null);



    }


}
